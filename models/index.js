const { User } = require('./user');
const { Question } = require('./question');
const { Answer } = require('./answer');
const { UserFeedback } = require('./user-feedback');

module.exports = { User, Question, Answer, UserFeedback };